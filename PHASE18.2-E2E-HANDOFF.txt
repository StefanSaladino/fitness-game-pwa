Top Set Phase 18.2 - deterministic Playwright preview

Purpose:
- Test-harness-only change.
- Prevent local Playwright runs from silently reusing an existing/stale server on port 4173.
- No production/UI/Supabase/scoring/recovery changes.

Changed file:
- playwright.config.ts

Behavior:
- webServer.reuseExistingServer is now false.
- The E2E gate must start the freshly built preview server.
- If port 4173 is occupied, Playwright will fail explicitly instead of testing the wrong server.
