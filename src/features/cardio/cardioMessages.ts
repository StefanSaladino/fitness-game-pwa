export function toUserFacingCardioError(error:unknown):string{const message=error instanceof Error?error.message:String(error??'');if(/Authentication required/i.test(message))return 'Your session expired. Sign in again.';if(/Cardio activity not found/i.test(message))return 'That cardio entry is no longer available.';if(/duration/i.test(message))return 'Check the cardio duration and try again.';return 'Cardio could not be updated. Try again.'}
