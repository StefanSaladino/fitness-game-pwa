Top Set Phase 18.2 — E2E boot + completed-set control refinement

Local-only overlay. No GitHub push, deployment, or Supabase migration.

Changes:
1. Completed expanded set controls:
   - Set number remains left.
   - Done and Collapse are grouped on the right.
   - Collapse sits immediately to the right of Done.
   - Collapse target is 44x44 px on mobile; chevron is 0.9rem.
   - Completed rows no longer need an extra vertical control row.
2. WorkoutSetList regression test asserts Collapse follows Done in DOM order.
3. Playwright production preview remains fresh (reuseExistingServer=false) and now clears
   VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY, and VITE_APP_URL for the E2E build.
   This prevents local production-preview E2E from contacting/rejecting the real Supabase backend.
4. WebKit header geometry test waits for the Messages trigger in the mobile portal slot and
   polls stable non-overlapping bounding boxes.

Recommended validation after extraction:
  npm run typecheck
  npx vitest run src/features/workout/components/WorkoutSetList.test.tsx
  npm run test:e2e

If E2E is green, continue:
  npm run test:internal
  npm run db:test:ci

Do not apply the Phase 18.2 Supabase migration or push yet unless explicitly approved.
